query Hello($name: String!) {
  hello(name: $name)
}

query GetMe {
  me {
    email
    firstname
    id
  }
}

mutation SignUp($data: SignupInput!) {
  signup(data: $data) {
    accessToken
    refreshToken
    user {
      email
      firstname
      id
    }
  }
}